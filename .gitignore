managed_components
build
build.clang
.history
.vscode
.cache
# Note: components/* are now Git submodules and should be tracked
# Only ignore submodule contents if needed, but submodule references are tracked
